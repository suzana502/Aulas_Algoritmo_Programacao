algoritmo "Enquanto Exemple"

var

 cont, soma: inteiro
// f: literal
 
inicio
  soma <- 0
  enquanto (cont < 1000) faca
       se (cont mod 2 = 0)   entao

          soma <- soma + cont
           
       fimse
       cont<- cont + 1
  fimenquanto
  
    escreval ("a soma dos pares e: ", soma)

fimalgoritmo
